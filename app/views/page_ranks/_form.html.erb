<%= form_with(model: page_rank, local: true) do |form| %>
  <% if page_rank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_rank.errors.count, "error") %> prohibited this page_rank from being saved:</h2>

      <ul>
        <% page_rank.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rank %>
    <%= form.number_field :rank %>
  </div>

  <div class="field">
    <%= form.label :link_id %>
    <%= form.text_field :link_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
